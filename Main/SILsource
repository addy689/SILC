decl
		integer x,y,z;
		integer swap(integer a,b);
enddecl

integer swap(integer a,b){
	decl
		boolean k;
	enddecl
	
	begin
		x[k]=k[k];
		a=b;
		b=z;
		return 1;
	end
}

integer main(){
	decl
	integer s;
	enddecl
	
	begin
		read(x);
		read(y);
		write(x);
		write(y);
		return 1;
	end
}
